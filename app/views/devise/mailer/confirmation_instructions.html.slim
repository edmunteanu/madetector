p = t('devise.mailer.confirmation_instructions.welcome', email: @email)
p = t('devise.mailer.confirmation_instructions.instructions')
p = link_to t('devise.mailer.confirmation_instructions.confirm'),
        confirmation_url(@resource, confirmation_token: @token)
